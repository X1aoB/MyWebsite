# 个人网站优化交付

2026-09-09。本轮实现已完成，保留“尘尘的白 / 苹果与狼”、角色插画、个人文案、原有栏目与链接、Astro 静态构建及 Cloudflare Pages 部署方式。前述本地验收阶段未发布生产站；现按用户上线指令，通过推送 `main` 触发 Cloudflare Pages 生产发布。未新增项目依赖。

本地生产预览：[http://127.0.0.1:4322/](http://127.0.0.1:4322/)。重新启动可运行 `npm run preview -- --host 127.0.0.1 --port 4322`；修改源文件后先执行 `npm run build`。

生产地址：[xiaob.dev](https://xiaob.dev/)。发布前完整测试再次通过（11 个文件、68 项）；类型检查 78 文件无诊断，生产构建 31 页。本报告中 `.codex-runtime/` 的截图与检查记录仅保存在本地，不随 Git 提交或站点公开。

## 最新修正：小吉终端、独立 Now 页面与公开信息

2026-09-09。本段是当前交付状态，替代下方记录中已经过时的项目名称、版本、截图和 Now 组织方式。

- **小吉终端**：全站可见品牌统一为“小吉终端”，英文为“Xiaoji Terminal”；既有项目 URL、日志锚点与 GitHub 仓库地址保持兼容。当前公开体验为 **0.10.0-rc.3**，面对面已加入 **22 位角色、每位 18 种基础表情**，并包含部分角色的特别表现、窄屏输入与角色切换改善、设置中的减少动效选项。
- **新版截图与说明**：五张新截图分别展示面对面、文字通讯、反馈、设置、公告。原始 PNG 完整保存、不裁剪，第二张保留浏览器地址栏；构建输出 **15 张 480/960/1440px WebP**，首页与项目页复用响应式预览，点击可查看原图。技术栈、数据处理、资料来源与许可均恢复为直接可读的完整说明。
- **搜索动画修复**：保留搜索展开、收起、输入区反馈与建议列表动画。收起时先停止交互，待淡出和上移完成后再隐藏；建议内容保留至淡出结束，避免提前清空导致动画中断。连续开关可自然反向过渡；减少动态效果时关闭动画。打开后立即聚焦输入框，保留键盘选择与两级 Escape 退出。
- **独立 Now 页面**：日常分享、面试经历、技术文章、随机攻略四个分类实际跳转到独立目录，当前计数依次为 **1 / 1 / 1 / 0**。两篇旧记录保留原文与原日期，并拥有各自文章页；技术分类链接真实的 journal 技术文章，攻略目录保持空状态，不补写虚构内容。旧 hash 入口继续兼容。日常分类英文同步为 Daily sharing，原 `/now/daily/` 地址保持不变。
- **公开个人信息**：恢复原先公开的个人介绍及 Bilibili、Steam、邮箱入口，移除 LinkedIn；本轮没有新增未经提供的个人资料。
- **左栏外框**：发布前按追加要求，将桌面左侧资料、导航和 GitHub 入口整体放入 16px 圆角卡片，使用主题底色、细边框和柔和阴影；宽屏左右栏同为 224px，平板左栏为 210px，原有内部内容宽度保持。外框不裁切搜索浮层或友情链接，手机继续使用顶部导航布局。

| 当前检查 | 结果 |
|---|---|
| Astro 类型检查 | 78 文件，0 errors / 0 warnings / 0 hints |
| Vitest | 68 项通过 |
| 生产构建 | 31 页，189 张优化图片（摄影 174 张 + 新截图 15 张） |
| 本地链接与资源 | 1,416 个引用，无缺失文件 |
| 可见品牌与社交链接 | 旧品牌出现次数 0；LinkedIn 链接 0 |
| 新截图原文件 | 5 张 PNG，复制前后文件哈希一致 |

浏览器已确认搜索即时聚焦、Now 分类进入目录再进入文章、旧 hash 跳转，以及 2559px 原图打开、Esc 关闭和焦点恢复。修正恢复社交入口后 768px 页脚的 7px 横向溢出，并补齐英文页面标题、图片替代文本及无障碍标签。布局修正构建在双主题 × 360/390/768/1280/1440px × 首页/项目/Now/面试目录/关于页共 **50 组**布局检查中均无横向溢出；英文项目、分类、文章及窄屏页脚另行复验通过。关键视口截图保存在下方索引中。随后按文案纠正将日常分类改为“日常分享”，恢复搜索动画并修正收起中断；对应 11 项搜索与 Now 测试以及 31 页完整构建通过，动效检查记录另存为 `search-motion-checks.json`。下方 Now 截图保留文字纠正前的布局快照，分类名称以当前预览为准。

[当前静态验收记录](.codex-runtime/verification/corrections-static.json) · [最终浏览器矩阵](.codex-runtime/verification/corrections-matrix.json) · [本轮截图索引](.codex-runtime/verification/corrections-screenshots.html)

本轮没有重新运行 Lighthouse。下方分数、LCP、gzip 体积与性能截图均为之前构建的历史测量，不代表当前新版内容的实测结果。

## 上一轮调整记录（已被上方最新修正替代）

以下保留 2026-09-09 上一轮调整的过程记录。其中 **0.9.6 公开版、候选验收、新界面开发中、0.9.2 存档截图、默认折叠 Now 分类与折叠技术说明均已过时**，请以上方当前交付为准。

- 左侧栏恢复纯导航，移除新增搜索/设置操作区与独立滚动条。较矮桌面窗口随整页滚动；搜索保留 Ctrl/Cmd+K，外观设置保留原顶部浮层入口，手机顶部控件均为 44px。
- 首页项目区改为体验介绍、真实界面存档预览和三阶段进度；项目详情依次展示双体验、当前进度、截图、上手步骤、折叠技术与数据说明。
- 依据“全面审查并优化项目”任务的最新可读进度，公开体验标为 **0.9.6**，兼容更新标为**验收中**，小吉终端新版界面标为**开发中**。四张原有截图明确为 **0.9.2 存档**。进度统一在 `src/config/project-snow.ts` 维护，首页、项目页、右栏、发现与状态入口同步。
- Now 按**日常分享、面试经历、技术文章、随机攻略**显示四个默认收起的大板块；现有两篇记录的双语原文与时间保持不变。支持键盘展开和分类直达链接；窄内容栏改为单列。空分类保留页面入口，但不加入搜索；分类搜索日期取真实记录时间。

上一轮验收：Astro 检查 **73 文件无诊断**，**66 项测试通过**，生产构建 **25 页**，**1,176 个本地引用无缺失**。首页、项目页、Now 在双主题 × 360/390/768/1280/1440px 共 **30 组**检查无横向溢出；保存了 12 张双主题截图。另验证了 Now 键盘展开、分类直达、图片 Esc 关闭与焦点恢复、手机设置退出、矮窗口导航以及英文窄栏。

[上一轮双主题截图](.codex-runtime/verification/revision-screenshots.html) · [上一轮布局记录](.codex-runtime/verification/revision-matrix.json) · [上一轮构建引用与搜索日期检查](.codex-runtime/verification/revision-static-check.json)

下方性能数据和原全面优化验收保留为前一阶段的测量记录；本次内容与布局调整没有重新运行 Lighthouse，不将旧快照冒充本次结果。

## 初轮全面优化记录

- **布局与视觉**：1200px 起三栏、761–1199px 两栏、760px 及以下单栏；收紧首页装饰横幅，调整角色插画、内容栏、标题与卡片比例。桌面搜索和设置始终可达，手机保留头像、搜索、设置与菜单，并独立展开外观设置。
- **动效**：晶体漂浮与轨道旋转、麦穗与苹果装饰、角色轻浮与细微视差、卡片反馈和轻量入场。主题切换使用独立扩散层；内容直接显示，后台及离开视口的装饰暂停，减少动态效果时关闭装饰运动。
- **搜索**：Ctrl/Cmd+K、方向键选择、Enter 打开、两级 Esc 退出、中文组合输入保护、失败重试及过期请求保护。标题匹配优先，JSON 与模型 IQ 已纳入搜索；工具不伪造日期，也不加入 RSS 或更新流。
- **阅读**：预计阅读时间、真实 H2/H3 目录、活动章节、章节链接、代码复制与回顶部。文章始终在同项目内连续阅读，单篇项目没有跨项目邻居；未归属项目的文章独立按日期和稳定 ID 排列。
- **摄影**：上一张/下一张、计数、键盘切图、未放大时的水平滑动、边界禁用、切图重置缩放与双层弹窗焦点恢复。58 张原图生成 174 个响应式版本；未打开的套图不加载内部照片，大图保留原文件。
- **JSON 工作台**：`/tools/json/` 提供校验、2/4 空格格式化、压缩、示例、清空、复制与下载。保留原始数字精度、重复键和转义；输入不上传、不保存。1 MiB 输入、64 层嵌套及 4 MiB 输出保护；超过 64 KiB 时按需启用 Worker，支持取消、超时和失败恢复。
- **模型 IQ**：部署 CSP 放行实际摘要域名，筛选保留焦点，说明随排序更新，提供加载失败与重试状态。
- **维护**：拆出摄影、项目、模型 IQ 和阅读专属样式；英文字典按需加载，中文首屏复用静态 HTML；滚动更新按帧调度。更新 sitemap、工具元数据、隐私说明和维护文档。

## 初轮验收结果

| 检查 | 结果 |
|---|---|
| Astro 类型检查 | 71 文件，0 errors / 0 warnings / 0 hints |
| Vitest | 10 个测试文件，66 项通过 |
| 完整生产构建 | 25 页，174 张响应式图片 |
| 本地链接与资源 | 检查 1,191 个引用，未发现缺失文件 |
| 核心布局 | 两主题 × 360/390/768/1280/1440px × 六类页面，共 60 组，无横向溢出 |
| 最终生产截图 | 双主题在 390/1440px 复验并保存 24 张截图 |
| 英文布局 | 六类页面在 390/1440px 均无横向溢出；原中文正文保留 `lang="zh-CN"` |
| 辅助页面 | 工具目录、IQ、日志目录、Now、更新、隐私、状态、搜索及 404 在 360/1440px 检查通过 |

浏览器中已验证：文章中途打开搜索并用键盘进入工具、立即聚焦、两级 Esc、快速主题切换；搜索首次 503 后重试恢复；存储读取被拒绝后仍可切主题和搜索；代码复制与目录跳转的焦点/当前小节；相册直达、首尾按钮、缩放重置、双层退出及损坏 hash；JSON 大整数、转义、无效语法、输入超限、复制和生产 Worker；模型 IQ 在真实部署 CSP 下成功加载、筛选保留焦点及排序文案更新。

无 JavaScript 验收使用禁止脚本的同源 sandbox iframe，确认首页、移动导航、相册与照片直链仍可访问。仅测试外框允许同源 iframe；被测页面仍使用真实生产 CSP。存储失败、搜索 503 和文章复制样例由只读测试路由注入，未写入源码或生产构建。

减少动态效果另有 6 项临时脚本/CSS 验证通过，包括初始与运行时切换、视差复位、后台暂停及实际媒体规则。测试文件和原始记录位于 `.codex-runtime/verification/`。

## 历史性能对比（本轮未重新测量）

以下记录来自 2026-09-09 上午全面优化阶段的构建快照，早于上方最新修正。基线来自 Git HEAD `a863c2ba5ee30e65e9eb4d8abc2cc9f41eff5e46` 的独立构建。首页在同一设备、相同生产头部与 gzip 下交替测量，基线和优化快照各 3 轮，下表使用各指标中位数。

| 指标 | 基线 | 优化快照 |
|---|---:|---:|
| Lighthouse Performance | 99 | 100 |
| 移动端 LCP | 1.806 秒 | 1.365 秒 |
| CLS | 0 | 0 |
| TBT | 0 ms | 0 ms |

LCP 减少 **24.42%**。后续 10:46 构建单次确认仍为 100 分、LCP 1.364 秒、CLS 0；最终搜索焦点和无脚本降级修正晚于该性能快照，最终文件体积另行统计，未混入三轮中位数。

该阶段最终产物的共享 CSS gzip **20,449 B**，全部初始 JS（含静态依赖）gzip **7,359 B**，合计 **27,808 B**。相对原计划约 44.5 KB 减少 **37.51%**；按补齐基线依赖的相同口径，44,925 → 27,808 B，减少 **38.10%**，超过 25% 目标。HTML gzip 另计 8,500 B；英文字典不在中文首次加载中请求。这些体积没有根据本轮最终构建重新统计。

摄影 480px 版本合计约 1.96 MB，对比原图合计 10.34 MB 减少约 81%；960px 版本合计约 5.29 MB。这里是同批图片的资源体积比较，实际页面只请求当前需要的尺寸与照片。

配置为 Lighthouse 13.4.1、Chrome 152，412×823、DPR 1.75、模拟 RTT 150ms / 1,638.4Kbps、CPU ×4。这些是本机首页实验室数据，不是线上真实用户数据；未测 INP。完整配置、7 份原始 JSON 和 Windows 临时目录清理警告见[性能报告](.codex-runtime/verification/performance-report.md)。

## 历史截图与复核资料

[浏览全部双主题截图](.codex-runtime/verification/screenshots.html)

| 页面 | 冰主题桌面 | 暖主题手机 |
|---|---|---|
| 首页 | [截图](.codex-runtime/verification/ice-1440-home.png) | [截图](.codex-runtime/verification/holo-390-home.png) |
| 长文章 | [截图](.codex-runtime/verification/ice-1440-project-snow-data-foundation.png) | [截图](.codex-runtime/verification/holo-390-project-snow-data-foundation.png) |
| 摄影集 | [截图](.codex-runtime/verification/ice-1440-gallery.png) | [截图](.codex-runtime/verification/holo-390-gallery.png) |
| Project Snow | [截图](.codex-runtime/verification/ice-1440-project-snow.png) | [截图](.codex-runtime/verification/holo-390-project-snow.png) |
| JSON | [截图](.codex-runtime/verification/ice-1440-json.png) | [截图](.codex-runtime/verification/holo-390-json.png) |
| 关于 | [截图](.codex-runtime/verification/ice-1440-about.png) | [截图](.codex-runtime/verification/holo-390-about.png) |

验收边界：中文输入法组合事件保护已审查，未使用真实系统输入法候选窗；滑动方向与阈值有单元验证，未用实体触屏；减少动态效果为脚本和 CSS 验证，未切换操作系统设置。JSON 下载点击后的成功提示已验证，但内置浏览器未返回下载事件，因此未确认文件落盘。Safari/Firefox 和线上慢网表现尚未实机覆盖。

源码与文档留在工作区，未提交或推送。截图、测试夹具、基线快照与性能原始文件保存在 Git 忽略的 `.codex-runtime/` 中，方便本地复核。
