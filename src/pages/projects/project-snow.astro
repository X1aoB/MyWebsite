---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { site } from "../../config/site";
import Giscus from "../../components/Giscus.astro";
---

<BaseLayout title="Project Snow" description="从可追溯数据底座进入本地 RAG MVP，验证尘白禁区角色对话的检索、引用与语境控制。">
  <section class="section">
    <div class="container project-hero">
      <article class="project-summary" data-reveal>
        <div class="eyebrow">PROJECT / 01</div>
        <h1>Project Snow</h1>
        <p>基于 RAG 架构的《尘白禁区》角色陪伴型对话系统。数据链路已经从采集进入本地 MVP：现在验证证据检索、角色专属资料优先级、对话模式和装甲 / 时装语境；在线服务仍未部署。</p>
        <div class="button-row" style="margin-top: 24px">
          <a class="button" href={site.repositories.projectSnow} target="_blank" rel="noreferrer">查看 GitHub 源码 ↗</a>
          <a class="button button-secondary" href="/journal/project-snow-data-foundation/">阅读项目总览</a>
        </div>
      </article>
      <aside class="status-panel" data-reveal>
        <div>
          <span class="status-badge">本地 MVP 验证中</span>
          <strong>检索、引用与角色语境已跑通</strong>
          <p>Project Snow/App 已提供 FastAPI 本地接口和静态预览；检索结果保留原始页面引用，关系候选仍需人工审核，不会自动写入正式图谱。</p>
        </div>
        <div class="tag-list"><span class="tag">FastAPI</span><span class="tag">Retrieval</span><span class="tag">Evidence</span><span class="tag">Local MVP</span></div>
      </aside>
    </div>
  </section>

  <section class="section-tight" aria-labelledby="pipeline-title">
    <div class="container">
      <div class="section-header" data-reveal>
        <div>
          <div class="section-kicker">系统链路</div>
          <h2 id="pipeline-title">从可追溯资料到本地对话验证。</h2>
        </div>
        <p>采集和状态治理已经形成基础；应用层继续构建 lakehouse、索引、证据画像和关系审核，MVP 对话只读取本地运行时产物。</p>
      </div>
      <div class="pipeline">
        <article class="pipeline-step" data-reveal><span class="step-number">01 / done</span><h3>页面发现与采集</h3><p>从受控入口发现 Wiki 页面，保存原始 HTML 或维基文本，不下载媒体二进制。</p></article>
        <article class="pipeline-step" data-reveal><span class="step-number">02 / done</span><h3>状态、清单与增量运行</h3><p>以哈希、抓取状态、运行报告和索引文件支持增量更新、分批恢复与目录校验。</p></article>
        <article class="pipeline-step" data-reveal><span class="step-number">03 / now</span><h3>证据检索与意图重排</h3><p>已生成 lakehouse、词法索引和本地向量索引；偏好、关系、语音、经历等问题会优先角色直接证据，并保留页面引用。</p></article>
        <article class="pipeline-step" data-reveal><span class="step-number">04 / now</span><h3>对话模式与角色语境</h3><p>MVP 支持沉浸式陪伴与角色助手两种模式，会话隔离；消息可自动识别装甲 / 时装并关联对应证据。</p></article>
        <article class="pipeline-step" data-reveal><span class="step-number">05 / next</span><h3>独立受控 RAG 服务</h3><p>未来再独立部署在线体验，补齐匿名限额、来源引用、服务端密钥和滥用防护；长期使用引导本地部署。</p></article>
      </div>
    </div>
  </section>

  <section class="section" aria-labelledby="roadmap-title">
    <div class="container split-panel">
      <div data-reveal>
        <div class="section-kicker">路线图</div>
        <h2 id="roadmap-title">从资料收集到可引用的角色回应。</h2>
        <div class="timeline" style="margin-top: 24px">
          <article class="timeline-card"><span class="timeline-phase">已完成</span><div><h3>原始数据采集与溯源</h3><p>覆盖剧情、角色资料、皮肤、语音、心意内容、家具和物品背景；保留页面地址、哈希、分类和运行状态，支持增量更新。</p></div></article>
          <article class="timeline-card"><span class="timeline-phase">已验证</span><div><h3>本地检索与 MVP 对话</h3><p>应用层已生成 lakehouse、词法 / 向量索引和角色证据画像；对话返回引用，并按问题意图优先角色专属邮件、语音、个人故事和随机事件等证据。</p></div></article>
          <article class="timeline-card"><span class="timeline-phase">进行中</span><div><h3>关系图谱人工审核</h3><p>确定性关系可供查询，模型抽取的关系候选进入分批审核；候选和二次模型意见都不会自动升级为正式图谱边。</p></div></article>
          <article class="timeline-card"><span class="timeline-phase">下一阶段</span><div><h3>独立在线体验</h3><p>后续再接入受控模型服务和公网入口；每次回答保留可访问来源，并在服务端处理限流、Turnstile、密钥和成本控制。</p></div></article>
        </div>
      </div>
      <aside class="license-note" data-reveal>
        <strong aria-hidden="true">※</strong>
        <div>
          <strong>来源与许可</strong>
          <p>项目当前数据来源为<a href={site.external.bWiki} target="_blank" rel="noreferrer">尘白禁区 BWiki ↗</a>。默认许可证为 CC BY-NC-SA 4.0，页面特殊说明优先；游戏及原始内容版权归其权利人所有。本站不分发原始采集数据。</p>
        </div>
      </aside>
    </div>
  </section>

  <section class="section-tight">
    <div class="container notice-panel" data-reveal>
      <div class="section-kicker">本地验证 / 在线服务</div>
      <h2>本地 MVP 已可运行，公网 RAG 尚未上线。</h2>
      <p>在 Project_Snow/App 中可以启动 FastAPI 与静态预览，使用本地运行时数据验证检索、引用和两种对话模式；公开服务后再补齐匿名限额、Turnstile、服务端密钥和滥用防护。长期使用仍以 GitHub 本地部署为主。</p>
      <div class="button-row" style="margin-top: 18px"><a class="button button-secondary" href={site.repositories.projectSnow} target="_blank" rel="noreferrer">查看项目运行说明 ↗</a></div>
    </div>
  </section>
  <Giscus />
</BaseLayout>
